// //* ARRAYS
`Arrays are iterable, they can be used with a for of loop`
const array = [1, 2, 3, 'Smita', {age:24}];
console.log(array[0]); 
array.push(5);  // Push the elemnet to the last.
console.log(array.length); // Length of the array.
array.pop(2);  //Removes Data from the 
array.shift(2); // REmoves the Data from the first


console.log(array);

