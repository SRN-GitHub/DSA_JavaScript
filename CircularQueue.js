`It uses a single block of memory to store elements.
It allows for efficient utilization of memory space by reusing empty slots.
The circular queue is also referred to as circular buffer or ring buffer.
Implemented with arrays, a circular queue will have circular structure.
During the dequeue (dq) operation, a block is created
A circular queue is an efficient data structure for implementing queues with a fixed maximum size
Enqueue adds elements to the rear/tail of the collection.
Dq removes elements from the front/head of the collection.`
