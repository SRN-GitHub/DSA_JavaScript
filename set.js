`Set is a data structure for holding unique values  
Arrays can contain duplicate values, but sets cannot.
Insertion order is maintained in arrays, unlike in sets.
`

const set = new Set([1, 2, 3]);
set.add(4)   // to add data inside set
console.log(set.size);

console.log(set)
console.log('Hi')